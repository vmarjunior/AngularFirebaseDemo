// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC0fbg_H_IfXalEMjKpWzlq6idDzbEo6BI",
    authDomain: "fir-demo-9cdda.firebaseapp.com",
    databaseURL: "https://fir-demo-9cdda.firebaseio.com",
    projectId: "fir-demo-9cdda",
    storageBucket: "fir-demo-9cdda.appspot.com",
    messagingSenderId: "8517496083",
    appId: "1:8517496083:web:f967bd887e46ca084afee6",
    measurementId: "G-GJ0ZYN46DD"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
